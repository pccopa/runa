import { EmptyCollectionState } from "@/features/collection-dashboard/EmptyCollectionState";

/** Vista inicial del workspace de colección (panel derecho sin request abierto). */
export function CollectionHomeView() {
  return <EmptyCollectionState />;
}
